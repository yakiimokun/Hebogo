# AGENTS.md

このファイルは、このリポジトリで作業する AI エージェント向けの開発ガイドです。

## プロジェクト概要

Hebogo は、Python の Tkinter で実装された囲碁 GUI アプリケーションです。対局エンジンには KataGo を GTP（Go Text Protocol）経由で使用します。

- エントリーポイント: `main.py`
- GUI 本体: `src/go_game_app.py`
- 対局設定ダイアログ: `src/settings_dialog.py`
- KataGo/GTP 連携: `src/gtp_client.py`
- 画像アセット: `blackstone.png`、`whitestone.png`
- 設定例: `config.sample.json`
- テスト: `test/`

## セットアップと実行

Python 3 を使用してください。依存パッケージは次のコマンドで導入します。

```bash
python3 -m pip install -r requirements.txt
```

`config.sample.json` を参考に、リポジトリ直下へ `config.json` を作成し、KataGo の実行ファイル、モデル、設定ファイルのパスを指定します。ローカル固有のパスや認証情報を含む `config.json` はコミットしないでください。

```json
{
  "katago_path": "/path/to/katago",
  "model_path": "/path/to/model.bin.gz",
  "config_path": "/path/to/gtp_setting.cfg"
}
```

アプリケーションはリポジトリ直下から起動します。画像や設定ファイルを相対パスで読み込む実装があるため、作業ディレクトリを変更しないでください。

```bash
python3 main.py
```

## テストと検証

テストには標準ライブラリの `unittest` を使用します。

```bash
python3 -m unittest discover -s test -v
```

テストの読み込み時に `src/__init__.py` 経由で Tkinter も読み込まれるため、Python 環境には Tk サポートが必要です。また、現時点の `test/test_score.py` はリポジトリに存在しない `src.board_utils` を参照しています。関連実装や実行環境が整うまではテスト収集が失敗するため、失敗を既存課題として報告し、依頼範囲外でテストを削除したり、内容を弱めたりしないでください。

GUI や KataGo 連携を変更した場合は、自動テストに加えて次を確認してください。

- 設定ダイアログを開閉できること
- 9路、13路、19路の盤面を開始できること
- 人間の着手、パス、投了が正しく反映されること
- AI 対局時に GTP プロセスが起動し、終了時に確実に停止すること
- KataGo のエラーや不正な応答で GUI が固まらないこと

## 実装方針

- 既存コードに合わせ、Python では 4 スペースのインデントを使用します。
- 公開クラスや複雑な処理には、簡潔な日本語の docstring またはコメントを付けます。
- UI 文言の表記を変更するときは、同じ画面内で大文字・小文字と言語を統一します。
- Tkinter のウィジェット更新はメインスレッドで行います。バックグラウンドスレッドから直接 UI を操作しないでください。
- GTP の送受信を変更するときは、成功応答、エラー応答、複数行応答、`pass`、`resign`、プロセス終了を考慮します。
- 外部プロセスやスレッドには、正常系と異常系の両方で終了処理を用意します。
- KataGo、モデル、設定ファイルへの絶対パスをソースコードへ埋め込まないでください。
- 画像ファイルや設定ファイルの配置を変える場合は、起動方法とパス解決も同時に更新します。

## 変更時の注意

- 変更前に `git status --short` を確認し、利用者の未コミット変更を上書きしないでください。
- 依頼に関係のない大規模な整形やリファクタリングは避けてください。
- 新しい依存関係を追加した場合は `requirements.txt` を更新し、追加理由を説明してください。
- 振る舞いを変更した場合は、可能な限り `test/` に回帰テストを追加してください。
- `Makefile` は過去の Swift 実装を参照しており、現在の Python 構成とは一致しません。明示的な依頼がない限り、`make` を標準のビルド・テスト手順として扱わないでください。

## 完了条件

作業を完了する前に、変更差分を確認し、実行可能なテストを実施してください。最終報告には、変更内容、実施した検証、既知の未解決事項を簡潔に記載します。
